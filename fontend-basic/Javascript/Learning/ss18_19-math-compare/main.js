/**
 * Toán tử so sánh - p2
 * ===
 * !==
 */

var a = 1;
var b = '1';

console.log(a === b); // so sánh tuyệt đối (value + data type)

/**
  Hiểu hơn về câu lệnh điều kiện
  và phép so sánh
 */

var result1 = 'A' && 'B' && 'C';
console.log(result1); // in ra C

var result2 = 'A' || 'B' || 'C';
console.log(result1); // in ra A